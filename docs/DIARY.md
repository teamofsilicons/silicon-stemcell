# Building Silicon


## 4.0.7: Repairing the package home 4.0.6 left unconfigurable

4.0.6 returned update policy to Honeycomb by deleting `auto_update` from each Silicon's private package home. Honeycomb requires that setting, so every package command in a migrated home failed with `config.json is invalid JSON; repair it before continuing`. The first upgraded connection could not install `tos>iam` and the Silicon stayed disconnected; the interpreter's restore reported the Honeycomb failure with the command needed to reproduce it.

The migration now writes Honeycomb's own default instead of removing the setting, and a home already left without it is repaired before any package command runs. Later app and user preferences remain untouched, and a configuration the interpreter cannot parse is left for Honeycomb to report. The repair was verified against a copy of the affected home using the real Honeycomb CLI, and the regression is covered twice: a unit test for migration, repair, and unparsable configuration, and an end-to-end connection that starts from the exact state 4.0.6 produced, with the mock Honeycomb enforcing the real CLI's required setting.

Upgrading from 4.0.6 needs only `silicon update` and an interpreter restart; the bundle layout is unchanged.

[Silicon 4.0.7](https://github.com/teamofsilicons/silicon-stemcell/releases/tag/v4.0.7) was published as latest stable on 17 September 2026 at 21:30 UTC. All six archives reported `v4.0.7` and the intended runtime-only inventory, and all eight asset digests matched the verified CI files before publication. The local interpreter updated through `silicon update`, restarted on 4.0.7, and restored its Silicon: every configured app reinstalled at its latest Honeycomb release, with IAM 2.0.0, DM 0.9.2, Briefcase 1.1.0, Browser 0.2.4, and Waveform 0.1.2 all satisfying the `iam --json` contract.



## 4.0.6: Independent Honeycomb applications

Configured and registered canonical app IDs now install through Honeycomb on every connection with no version argument. Application executables, version selections, and update-suppressing wrappers have been removed from the interpreter bundle. Honeycomb itself is downloaded independently from its latest release with checksum verification, preserving existing update settings and services. Omni and Caddy remain runtime dependencies.

The per-Silicon package registry can coexist with unrelated global commands. Existing files inside the private command directory remain protected against collisions. An old interpreter-imposed Honeycomb update preference is removed once; subsequent preferences belong to the app or user. The 48-hour authentication cache remains separate from installation.

[All six platform builds](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35270308880) passed for runtime source `0116c15c31bb7564d55cff901bb008b4478fa9de`, including the real Omni/Caddy Unix suite and Windows x64 WSL2/native-command suite. Windows ARM64 remains preview pending physical ARM64 WSL2 testing. Local checks also installed IAM, DM, Briefcase, Browser, and Waveform without version arguments alongside existing global commands and verified their required CLI contracts. The reconnect regression proves installation runs again while a fresh authentication cache avoids reauthentication.

Upgrading from 4.0.5 or earlier requires running the new installer once because older embedded updaters require the former bundled app inventory.

[Silicon 4.0.6](https://github.com/teamofsilicons/silicon-stemcell/releases/tag/v4.0.6) was published as latest stable on 17 September 2026 at 20:53 UTC. All six archives passed version, inventory, native architecture, installer, and notice checks. All nine uploaded assets matched the verified files and GitHub’s SHA-256 digests. Transient upload failures left incomplete Windows assets; only those incomplete records were removed, and the verified Windows artifacts were uploaded from a temporary GitHub workflow, then rechecked before publication.

9–17 September 2026 · implementation diary

### 17 September: verified 4.0.3 bundles

The final source is `567894c23515396b14ef8d99354f9bb890cfa69b`. [Main checks](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35197785502), [all four native Unix jobs](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35197918153), and [both Windows jobs](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35198376479) passed. Unix checks include full installation and real Omni/Caddy E2E; Windows x64 includes actual WSL2 runtime and native-command checks, including the new authentication progress regression. ARM64 Windows remains preview pending a physical WSL2 run. All six archives passed content, architecture, notice, installer, and version checks; the Windows runtime archives exactly match their corresponding verified Linux bundles. Every uploaded asset matched GitHub's size and SHA-256 digest. Protected unpublished candidate tags were preserved; the final verified source has the new `v4.0.3` tag.

The public Unix one-liner installed 4.0.3 into a clean macOS ARM64 prefix, with source/version/mirror overrides removed. All 34 installed payload files matched the verified archive. All nine public asset URLs returned HTTP 200 without authentication. The guide includes both public one-line installers and the exact-source platform evidence.

### 17 September: live connection progress

`silicon connect` now follows new progress records in the existing redacted local log while its control request runs. Setup, app installation, authentication, and webhook registration report running, completed, or failed states. Terminal output replaces the pending status with a checkmark or failure marker; redirected text has no cursor controls, and JSON output remains a complete machine-readable response. No hosted realtime service or new streaming endpoint was added. A gated app authentication test proves output arrives before the app returns, then checks success, failure, credential-output suppression, and JSON mode through the actual CLI and daemon.

The broader tests exposed two fixture issues. The installer mock still expected Hook 0.6.0 after the bundle pin moved to 0.6.1; its expectation now matches. The heartbeat fixture retained its initial global session when switching to session addressing, accidentally scheduling a third heartbeat through a real Omni process. It now ends that initial session and asserts exactly two test targets. The interpreter scheduler did not need a production change.

### 17 September: readable webhook events

The reported `make_readable(request)` failure was a missing registration in the shared CEL context. `make_readable` now uses the existing YAML serializer so nested event data stays readable through `var` and `send`; `convert_time` aliases the existing timezone formatter. The reference YAML's three misplaced interpolation braces and Python null check were corrected. Regression checks evaluate all reference flow branches, nested and scalar data, and timezone aliases. The protocol E2E now also sends a DM-shaped webhook through `make_readable(request)` → `var.new_message` → the real Omni transport. Version 4.0.3 carries the fix across Unix and Windows/WSL. A fresh Honeycomb catalog check found Hook 0.6.1, so its bundle pin was updated before publication; IAM 1.11.0, Space Station 0.1.4, DM 0.7.0, Briefcase 1.1.0, Waveform 0.1.2, Commit 0.2.0, and Remind 0.2.0 matched their latest published versions.

### 17 September: GitHub distribution only

The interpreter does not need a Honeycomb listing. Its catalog publication plan was withdrawn, and the unused package-preparation script, matching test, and CI step were removed. GitHub Releases and the existing Unix/Windows one-liners remain the distribution path. Honeycomb still supplies application dependencies and managed app installation. No `tos>silicon` application, catalog release, or review request was created. Earlier Honeycomb packaging work below is historical, not outstanding release work.

### 16–17 September: preparing 4.0.0

The requested scope is the local Silicon interpreter, with Windows delivered through WSL2. The hosted realtime publisher, reader commands, backend source, and remote service were removed. Local ping, configuration inspection, the dashboard, managed applications, and Space Station telemetry remain. Version 4 is a major release because it removes the remote login/watch interface. Windows and publication checks will be recorded here as they complete. The reduced interpreter passed 36 unit tests, formatting, Clippy, and the full real Omni/Caddy end-to-end suite locally; two Caddy-specific unit tests remain separate from that suite. The hosted Windows probe ran WSL2 successfully on x64. Its ARM64 runner could not install WSL, so ARM64 runtime validation is explicitly incomplete.

The dedicated realtime AWS stack, systemd service and runtime files, artifact bucket and its versions, runtime secret, ACM certificate, and backend telemetry table were deleted. Both the service and certificate-validation DNS records return authoritative NXDOMAIN. The shared Space Station host, service, and load balancer remain intact; the existing service PID was unchanged. IAM/Honeycomb retirement and publication constraints are recorded in the external issue ledger.

The native Windows x64 launcher and actual WSL2 distribution passed the full real Omni/Caddy protocol suite, followed by native command checks for Unicode and quoted arguments, UNC paths, piped input, exit codes, persistent settings, connection lifecycle, redacted configuration, and Windows access to the local dashboard. The browser bridge opened the dashboard from `silicon web`. Both Windows architectures built their PE launchers and passed native unit tests; ARM64 WSL2 execution remains unverified. The first WSL run caught Windows checkout line endings in the Linux provisioning script; repository attributes now preserve LF for shell and Python files. [The successful integration run](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35134808415) used the initial Linux runtime; the final runtime and PowerShell 5.1 installer checks follow separately.

The user requested the latest application CLIs during testing. Anonymous Honeycomb downloads verified IAM 1.11.0, Space Station 0.1.4, DM 0.7.0, Briefcase 1.1.0, Waveform 0.1.2, Commit 0.2.0, Remind 0.2.0, and Hook 0.6.0, with matching native headers on all 32 Unix payloads. Honeycomb 0.2.3 is the current bootstrap release; Omni 0.7.2 and Caddy 2.11.4 remain current. The installer now obtains all eight application binaries through Honeycomb instead of mixing older crate and Git builds. A complete isolated local installation succeeded with those versions. Their published licenses and exact archive hashes are retained in the dependency notices.

Honeycomb packaging now exports only `silicon` and `si`, keeping the remaining tools private so a fresh installation does not collide with Honeycomb itself. Both interpreter entry points discover their versioned sibling bundle directory before starting threads, with duplicate paths removed and each Silicon home retaining command priority. A real native subprocess test checks discovery through the public symlink, private dependencies, working directory, home overrides, update suppression, and rejection of an unmarked directory.

The refreshed Windows run exposed an unbounded heartbeat backlog: a periodic tick could start another operation while the previous one still held the same session's provider connection. Slow WSL storage made that queue delay foreground commands and DNA updates. The scheduler now permits one heartbeat per connection generation and session, coalescing missed ticks while other sessions continue. Its regression blocks one provider RPC over multiple ticks, confirms independent-session progress, then confirms foreground delivery and complete draining. The final local checks passed 38 unit tests and the bundle-path integration test, with the two separately exercised Caddy tests omitted from the ordinary suite; formatting and Clippy passed. A fresh six-target release build will validate the combined changes.

All four native jobs in the [final Unix build](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35139170050) passed on source `83fbefe6ecfd93ab1c87ae8f2be9b18f23170247`, including installed-bundle integration and live application discovery. Every one of the 32 bundled application binaries matched the independently verified Honeycomb payload byte-for-byte. The workflow's duplicate dependent Windows jobs were cancelled after these four jobs completed; Windows was already running separately against the same Linux artifacts. That [Windows run](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35139664235) passed the full WSL2 interpreter suite in 17 seconds, including the previously starved heartbeat/DNA checks, then found a separate Windows browser launch failure (`Exec format error`). Its installer correction and public installation results are recorded after verification.

The final [Windows verification](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35140947343) passed on `8fa368dda55c6a4982640595694fcb425cc84e37`, using the unchanged verified Unix runtime from `83fbefe`. Fresh PowerShell 5.1 installation exercised the missing-only interoperability repair, then verified native PowerShell output and exit status. The full real Omni/Caddy WSL2 suite passed, `silicon web` opened the Windows browser, and native command checks passed for arguments, Unicode, pipes, exit status, configuration, settings, connections, ping, redaction, and shutdown. Both Windows ZIP files passed native packaging checks. ARM64 WSL2 remains an explicit preview pending suitable hardware. Public installer and documentation deployment verification follow publication.

The complete six-platform Honeycomb archive was prepared from the final checksummed release assets and passed both directory and archive validation with Honeycomb 0.2.3. It exports only `silicon` and `si`, retains all dependencies, and includes the Windows interoperability script and exact matching Linux runtime archives. Its 229 entries total 348,665,333 compressed bytes and 705,208,541 expanded bytes, within the catalog limits; SHA-256 is `99c75cb6b3ad17c39955b663a04a1412e8e0d8725b07283d56a766caa7757a8f`. This is a locally prepared package, not an uploaded or registered catalog release: the subsequent distribution decision removed catalog publication from scope.

[Silicon 4.0.0](https://github.com/teamofsilicons/silicon-stemcell/releases/tag/v4.0.0) was published as the stable GitHub release at tag `8fa368dda55c6a4982640595694fcb425cc84e37`. All nine uploaded asset sizes and digests matched, all eight checksum entries verified, and the six native/launcher archives passed content, architecture, installer, and license checks. All nine canonical download URLs returned HTTP 200 and matching sizes without authentication. The exact public Unix one-liner passed in a clean macOS ARM64 prefix, all 34 installed payload files matched, same-version reinstall repaired missing dependencies while preserving user YAML, and its full real Omni/Caddy E2E passed. The exact public PowerShell one-liner then passed fresh Windows x64 installation and all runtime/native checks in [run 35141926050](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35141926050). Only after those public checks was the candidate marked stable.

Task-owned local realtime credential fixtures and the revocation helper were removed. Credential-free cleanup IDs remain for the temporary test Silicon awaiting email verification and the retired app requiring an IAM platform operator. Honeycomb catalog registration and review were subsequently removed from scope; no catalog publication is needed.

### 16 September: implementing 3.6.0

The updated specification introduces explicit connection setup, canonical Honeycomb application IDs, a user Space Station destination, source-attributed DNA, persistent settings, and read-only organization updates. The preserved reference YAML now includes `setup`, `apps`, `webhooks`, and `space_station`. The interpreter accepts those fields while retaining the existing command-based authentication fields and legacy ISI mode mappings.

Compilation validates deferred expressions without invoking setup or application commands. Setup runs only when connecting, with stdout and stderr recorded; an exhausted fallback prevents the connection from starting. Application lists combine authentication and webhook requirements, resolve installed commands by verified IAM identity, and install missing packages through Honeycomb. Space Station's native CLI is acquired through a real Honeycomb package installation when constructing the distribution.

DNA assembly now includes each original entry before its computed text. Configuration regressions passed for deferred execution, canonical ID validation, telemetry schema, and unchanged source files; a separate runtime regression passed for source attribution and failed-entry skipping. Four configuration tests and that DNA test were run locally during implementation.

The integrated local interpreter run passed 34 tests, with two separately runnable Caddy tests ignored, and Clippy passed with warnings denied. The complete real Omni/Caddy protocol E2E passed, including setup, settings, redacted configuration, local ping, bug-report preview, and lifecycle checks. The documentation endpoint passed its Node regression. Installer checks covered missing new dependencies, failure preserving the selected release, and copying the portable Space Station executable from a real anonymous Honeycomb package installation. Space Station 0.1.3 requires an explicit organization at login; Silicon now supplies it from the configured ID. No upstream service was changed.

A new Silicon home with no saved Space Station organization completed real IAM 1.9 SLT exchange and canonical-ID authentication for the retained production test Silicon. Authenticated status returned the exact Silicon identity and organization; removal succeeded and the final status was unauthenticated. The complete native source installation also passed packaged install/uninstall and the real Omni/Caddy E2E, with all 16 command-line interfaces and 34 payload files present.

The final security review reproduced compile-time command logs exposing a table-key expression before runtime redaction was registered. Credential fields now use a dedicated evaluation mode that omits source and stderr from errors, all compile-time Bash omits its command body, and ordinary evaluation errors redact known token and table-key values. Four credential-focused tests passed, including two new regressions covering actual Bash and malformed CEL. Delayed logs now retain their original connection identity, cannot reach a replacement connection's telemetry table stream, and remain redacted after disconnect. Daemon request telemetry uses retained known credentials when sanitizing context, and log origins are redacted alongside messages. The final protocol E2E passed again before those last two diagnostic-only redaction changes.

The static guide rendered successfully and its 58 internal anchors resolved. Chrome checks passed for the desktop layout and a 390-pixel mobile viewport, including opening the mobile menu, navigating to settings, and closing the menu. The temporary local static server did not implement the telemetry API, so that layout check did not establish live ingestion success.

Live telemetry checks subsequently reached Space Station. An interpreter connected with TOS telemetry disabled wrote a runtime event to the Silicon's own configured table; a direct table query found the event with its token redacted. The Vercel documentation preview accepted a real `/api/telemetry` event, and the `silicondocs` table contained its `release_smoke` row with version `3.6.0`.

The vendor telemetry check confirmed the SDK's bounded-flush behavior: a standalone CLI queued its event, and starting an isolated interpreter drained the durable queue. The actual `siliconinterpreter` table contained both CLI and daemon records. The current interpreter suite then passed 40 tests with two Caddy tests separately runnable, Clippy remained clean, and the real Omni/Caddy E2E passed. A scan of 94 public tracked and untracked files found no matches for the private deployment credentials.

Cross-organization consent exposed a separate IAM defect: a Carbon owning both organizations could obtain a token for the app's organization but received HTTP 500 when choosing its other organization. The failure reproduced in production and in a fresh isolated testing environment after scope consent. The external issue ledger records the exact commands and request IDs. Silicon rejects the failed login; no upstream service fix was made.

The documented quickstart and a full example including setup, application IDs, webhooks, and the optional Space Station destination were compiled in separate temporary homes. Both passed without running setup or changing YAML bytes. They use clearly marked credential placeholders for compilation; readers supply their own IAM and table credentials before connecting. The supplied stemcell remains an intentional sketch, as specified in `UNDERSTANDING.md`, and was not rewritten into a different user's runnable configuration.

The final Honeycomb compatibility check found no process-level automatic-update override. Package operations now use a private `<home>/.silicon/packages` directory with `auto_update=false`, while application credentials and command links retain the original home. This keeps the bundled Honeycomb executable under Silicon's updater and preserves personal Honeycomb configuration. After this change, all 40 interpreter tests passed with two separately runnable Caddy tests ignored; formatting, Clippy, and the installer's atomic-verification regression suite passed. The rendered guide contains 22 chapters and 59 valid internal anchor links. A real package install/uninstall confirmed the private registry and unchanged personal registry; a second check with the bundle on PATH returned `already_available` without changing the existing Space Station binary. Child Space Station processes disable their independent updater. Four-platform release CI and public release checks are recorded only after publication below.

The source was committed to `main` and tagged `v3.6.0` at `4205927`, starting the [first native release workflow](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35121278942). Both Linux ARM64 and x86-64 jobs passed interpreter verification, built the complete bundle, passed its real Omni/Caddy E2E, and executed the bundled CLIs. The required IAM discovery gate then rejected DM 0.3.0: `dm iam --json` could not parse the current API's response envelope and reported a missing `app_id`. Packaging and artifact upload were skipped. The same failure reproduced locally. Publication was paused while a compatible published dependency version was verified; no upstream code or service was changed to make the test pass.

The already-published DM 0.7.0 was built and verified against the actual service in a fresh home. Real IAM token issuance and interpreter authentication succeeded, status identified the exact retained Silicon and organization, webhook registration reported hooked, removal reported unhooked, and logout left the app unauthenticated. The distribution pin and provenance notice were updated to that published version and source commit `396df5a76f8bc9dea163d807638e8eac972ba8c0`. Release publication still awaited a fresh native build and the remaining live dependency checks.

The same dependency review found that Briefcase 0.2.4 no longer matched its current service contract. The published Briefcase 1.1.0 replacement was built from source `951d6ba09e8cb14b5c79eab043aab0a2c70ba7bf`. It reported matching client, server, and contract versions; discovery returned `tos>briefcase`, a fresh home reported unauthenticated, and both login and logout help were available. Its full live cycle then passed: IAM issued an SLT, login returned the exact retained Silicon identity and organization, status was authenticated, logout succeeded, and final status was unauthenticated. No content was written. The pin and provenance documentation were updated, with the existing Apache 2.0 license unchanged.

GitHub's repository rules rejected changing the unpublished `v3.6.0` tag; the associated atomic main-and-tag push was also rejected. The original failed tag remains at `4205927`. The corrected distribution advances to **3.6.1** with DM 0.7.0 and Briefcase 1.1.0. The corrected native source installation passed all nine application-discovery checks without changing the managed binary hashes, followed by the installed bundle's full real Omni/Caddy E2E. These local receipts precede the new candidate's four-platform CI and public-release verification; they do not claim either has completed.

The corrected complete source bundle passed all nine application discovery checks with unchanged executable checksums, then passed the full installed Omni/Caddy end-to-end suite. Direct Honeycomb use separately reproduced an upstream self-update replacing a managed command link; the external ledger records its reproduction and opt-out. Silicon-driven package operations and release checks disable that updater in their isolated homes.

A read-only audit of the published 3.5.1 updater confirmed a migration limit in Silicon itself. It executes the installer embedded in that older binary; the older installer extracts and links only its fixed dependency inventory, which lacks Honeycomb, Space Station, and their notices. Although the new installer is copied into the release directory, the old update path does not execute it. The README and install guide therefore require existing 3.5.x users to rerun the complete 3.6.1 public installer once into the same prefix, even if the interpreter already reports 3.6.1. The guide explains stopping and restarting the interpreter and preserving a custom prefix. This documentation correction requires no runtime change or new tag.

Chrome dashboard verification used a separate temporary interpreter home with vendor telemetry and automatic updates disabled by environment. The authenticated page loaded the settings available in that release. Turning telemetry and automatic updates off through their checkboxes and pressing Save returned the expected values; the separate `settings.json` confirmed they persisted. The App ID field, Install and Uninstall buttons, Ping selected Silicon, and Show configuration controls were present. No app installation was attempted. A later reload check was interrupted by Chrome's extension UI, so persistence evidence comes from the successful save response and stored file. The temporary dashboard tab and interpreter were closed, and its private authentication URL was removed.

The corrected `v3.6.1` candidate is tagged at `3f604bb1d99c2060b70377f6f8afe92693264475`. Its [release workflow](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35124053286) passed all four native jobs—macOS and Linux, each on ARM64 and x86-64—and completed the draft-release job. The separate [source checks](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/35124052640) also passed. Interpreter tests passed 40 checks with two Caddy-specific tests marked ignored in the ordinary suite; the native jobs separately passed installed-bundle E2E with real Omni and Caddy.

[Silicon 3.6.1](https://github.com/teamofsilicons/silicon-stemcell/releases/tag/v3.6.1) was published as the latest stable release on 16 September at 17:19:29 UTC. All six asset sizes and GitHub digests matched the downloaded files, all five checksum entries verified, and all four native archives had the exact 34-file payload, architecture, installer, notices, and version. The exact public curl installer then passed in a fresh macOS ARM64 prefix. A same-version reinstall repaired missing Honeycomb and Space Station executables, command links, and notices while preserving a marker YAML; all 34 installed payload files matched the verified native CI archive, and both new dependency commands ran. That public installation passed the full real Omni/Caddy E2E, including setup, settings, redacted configuration, ping, bug-report preview, routing, sessions, heartbeats, DNA refresh, restart, and shutdown.

Unauthenticated HEAD requests to all six canonical public asset URLs returned HTTP 200 and the expected Content-Length. The production documentation deployment served the exact rendered 3.6.1 HTML at [docs.teamofsilicons.com](https://docs.teamofsilicons.com), with the correct public installer link and migration instructions. Its live telemetry gateway returned HTTP 202 and accepted the verification event. The documentation follow-up preserves the immutable runtime tag and records the completed installation and deployment receipts.

The historical entries below describe the earlier 3.5 releases.

### 9–10 September: Silicon 3.5

The 3.5 work establishes one Rust interpreter and a shared control surface for terminal and web operations. Configuration paths identify local connections; Silicon IDs identify their routed local hosts. Real Omni processes replace file-based send placeholders, and inference events drive delivery receipts, progress logs, and session lifecycle.

The parser preserves the reference dialect's ordered flow while rejecting ambiguous ordinary duplicate keys. Compatibility warnings make legacy addressing/retention fields visible. Expression validation covers unselected branches and fallback candidates before compile-time Bash can run. Login and webhook entries use a distinct deferred-command evaluation mode so configuration such as `! dm` retains its intended app-command meaning.

Session work separates logical archive names from immutable Omni UUIDs. Persistent sessions retain metadata, conversation data, and history. Ephemeral sessions route a completed result to an internal caller and remove their recoverable data. Rollover creates a fresh persistent successor, preserves the predecessor's identity/timestamps, and retains the live address for session-mode work. Delivery and retirement tests cover callbacks, late failures, retries, and messages arriving around the end of a turn.

Scheduling evaluates DNA and heartbeat intervals at their documented boundaries. DNA assembly was moved off the provider event listener so refresh scripts do not block inference receipts. Suggestion counters exclude control messages and operate independently per session. Restart admission is coordinated with active dispatch and nested runtime work.

Authentication uses the installed IAM CLI's Silicon login contract, an isolated IAM state home, app discovery/status checks, and application-owned sessions. Dynamic app authentication is remembered for later session/heartbeat checks. Credential-bearing subprocess output is kept out of normal logs. The full authentication contract has controlled tests; live multi-app integration remains a separate verification task.

Caddy is owned by the interpreter and controlled through a private Unix socket. Route updates have rollback behavior and do not reconfigure an unrelated Caddy daemon. The installer builds or downloads complete bundles, verifies checksums, checks every required member, and changes the selected release only after preparation. Managed wrappers prevent dependency CLIs from independently updating the bundle.

Verification combines Rust tests, real Caddy integration, a real Omni protocol E2E with a scripted provider, and a separate actual Claude inference smoke test. The latter returned `SILICON_SMOKE_OK`. Release publication and documentation hosting should be recorded here only after they are complete and checked. No deployment claim in this diary is a substitute for that final evidence.


### What the final integration runs caught

The real IAM 1.4.1 executable requires an explicit organization grant when minting an app token. Selecting `--org` only chooses context; it does not grant app access. The interpreter now passes `--grant-org` for the Silicon's own organization, and the contract check asserts it.

Restart testing exposed a saved-session edge case: `end` initially found only loaded workers. It now archives persistent sessions directly from disk without starting a provider, accepts the logical ID or Omni UUID, and serializes loaded-session ending with message acceptance. The E2E creates a session, restarts, and ends that unloaded session by UUID.

The updater was exercised against complete local release bundles with the real interpreter and Omni. Two test seams replaced GitHub metadata HTTPS with a loopback fixture and shortened the one-hour interval; those seams exist only in the test source copy. Equal/older versions, drafts, prereleases, bad checksums, and unmanaged builds were rejected appropriately. Successful activation preserved YAML and connections. During active work, version 3.5.0 kept serving after the new bundle was installed; only after provider completion did it execute 3.5.1 with the same PID and port, restore the session UUID, and resume work. This found and fixed ordinary shutdown incorrectly marking completed work as an error. GitHub's live transport and the literal hour-long wait are outside that accelerated check.

Browser verification covered configuration validation and connection, sending and injecting into a running turn, displaying provider completion, persistent rollover, and archive filtering. Mobile checks caught a connection-table overflow and a navigation handler that returned `false` while closing the menu, cancelling its link. The table now stacks at narrow widths and mobile documentation links navigate normally. The dashboard also retains the initial URL-fragment token for its browser session.

The complete source install and packaged install were tested in isolated prefixes. All 16 payload executables/wrappers were present, pinned Omni and Caddy versions matched, six application discovery commands returned IDs, and the installed bundle passed the protocol E2E. Package extraction and relocation preserved payload bytes, notices, wrapper behavior, and installation metadata. This is separate from live app authentication and from the four-platform release build.


### Publishing the guide

The static guide is live at [docs.teamofsilicons.com](https://docs.teamofsilicons.com), hosted by the `silicon-docs` Vercel project. DNS verification added a TXT challenge and the `docs` CNAME; a before/after comparison confirmed that all 55 existing DNS records remained unchanged. The domain returned HTTP 200 with verified TLS, and Chrome displayed the published page. Mobile navigation, the installation copy button, and desktop layout were checked. The deployment uploads public documentation assets; template homes, Rust build output, and local environment files are excluded.

GitHub's branch checks passed on both Ubuntu and macOS. The first complete implementation commit is `3bfd07e` on `stemcell-v3.5`; the final runtime is tagged `v3.5.0` at `3ac2922`. The supplied `stemcell/` tree is byte-for-byte unchanged. The four native release jobs passed interpreter verification, installed-bundle E2E with real Omni/Caddy, CLI execution, and IAM discovery. Each resulting archive contains exactly 16 payload executables/wrappers, the version marker, installer, and license notices; no YAML or runtime state is included. All four downloaded release archives matched their native CI artifacts, and all six assets matched their checksums and GitHub digests. The release was published on 9 September 2026 at 11:35 UTC, with the pending shared IAM login issue explicitly recorded in its notes.


A final failure-path review caught provider errors that can arrive before any `START` or `END`. Terminal blocked workers now fail their receipts and retire, so an update is not postponed forever by work that cannot begin. Flow sends now wait for the delivery receipt within the action, allowing `send.catch` to handle that failure and continue before the event is acknowledged. A regression using the real Omni Rust client and a controlled Unix transport checks both the failure case and successful acknowledgment before turn completion.

Two lifecycle races were closed before release. A connection obtained just before disconnect must recheck its enabled state while holding the worker lock, preventing it from creating a daemon and capability after cleanup. Restart admission also counts the entire HTTP operation and heartbeat preparation, including shell evaluation and authentication. A real HTTP compilation test holds a Bash expression open, verifies that restart is refused, releases it, and verifies that restart becomes possible after the response. The final ordinary suite passed 24 tests, with the two separately exercised Caddy integration tests marked ignored.

### Tracing the real application logins

Briefcase, DM, Hook, Remind, Waveform, and Commit all exposed their IAM IDs and received IAM-issued short-lived tokens, then rejected the test Silicons during hosted login. Direct exchange controls made the failure precise: a test Carbon succeeded, while two test Silicons in separate owned organizations failed with `invalid_grant`. IAM's issuer deliberately creates unscoped selected-organization login grants, but its shared OAuth authority function rejected the same shape for Silicon subjects. That function also serves refresh and introspection, so changing one app or retrying a consumed token would leave the root cause intact.

The prepared upstream change is one forward SQL migration plus a regression in IAM's existing protocol suite. It preserves the exact application, consent, parent session, active Silicon, organization, and membership checks while accepting the issuer's unscoped grant. On disposable PostgreSQL 16, the regression fails against the old helper and the full protocol suite passes with the fix. Another 339 ordinary IAM tests, formatting, Clippy, migration-security, and runtime-grant checks passed. The rollback function was also exercised transactionally. Hosted deployment remains pending: IAM verifies an exact migration ledger, so the API/worker image and both production/testing schemas must move together. A local CLI URL override cannot redirect token exchanges made inside already-published app servers. Hosted success will be recorded after that coordinated rollout and the six login checks actually pass.

The accelerated updater check was rerun against the final runtime commit after the HTTP/heartbeat activity guards landed. Both idle and busy cases passed: the installed version changed, the process retained its PID and port, connections and YAML stayed intact, and the persistent session resumed through real Omni. The busy case held the old runtime until provider completion. This final run also rechecked release filtering, checksum failures, source/mirror override removal, and cleanup.

After publication, the actual public `curl` installer was run into a fresh macOS ARM64 prefix with source-build and mirror overrides removed. It downloaded the release bundle and installed every dependency without a source build. All 30 installed payload files matched the verified native CI archive, including all 16 executables/wrappers. That public installation passed the complete real Omni/Caddy protocol E2E. Its `silicon update` command also queried GitHub's live HTTPS release metadata and correctly returned no update for the already-current 3.5.0. This checks the real transport separately from the accelerated updater's documented test seams.

### 10 September: production first, then testing

IAM PR #19 was merged and deployed externally. The public version endpoint reported its exact merge commit, `b5b5537`, and readiness was healthy. The next authentication run reused the production organization and Silicon created the previous day, with the same released interpreter and bundled CLIs. All six production logins now passed. Five logout cycles passed too; Commit's published CLI had no logout command, so its valid session remained saved.

The paired testing environments were checked afterward. Briefcase, Hook, Remind, and Waveform passed immediately. DM saved a valid session but its login command exited unsuccessfully when its local relay could not bind an occupied port. The occupant was an orphan from our production test, identified by its executable, start time, and fixture working directory. Stopping that owned relay and retrying DM with a distinct available port produced a complete login/logout pass. Credential-directory isolation does not by itself isolate TCP listeners. Both the failed baseline and corrected run remain recorded. The test harness also used an obsolete argument for interpreter disconnect; the cleanup fallback stopped the owned process, and the harness was corrected to use `target`.

Commit's testing login still returned HTTP 401. A useful control was to reuse that exact, still-unconsumed short-lived token directly with IAM and the imported Commit test-app credential. Exchange succeeded, introspection identified the active Silicon, and revoking the resulting refresh family made it inactive. This isolated the remaining failure to Commit's backend: it selected the IAM testing key while retaining its production application credential. Its environment schema stored no paired application credential. The correction must cover login, refresh, logout, status, and protected-request authentication together.

The separate CLI omission was smaller. Commit's client library and backend already expose session revocation, so [Commit PR #1](https://github.com/teamofsilicons/silicon-commit/pull/1) adds a logout command that uses the saved refresh token and deletes its credentials only after the server succeeds. It preserves failed or malformed state and unrelated configuration. Its local regression and full GitHub CI passed. A separate candidate binary then removed the retained production session and passed a fresh interpreter-driven login/logout cycle; explicit checks with both previous access tokens confirmed server-side revocation. The published bundle was left unchanged.

[Commit PR #2](https://github.com/teamofsilicons/silicon-commit/pull/2) pairs each testing environment with its imported IAM application ID and encrypted secret. It reuses the existing encryption and lifecycle version checks, selects the pair for all session and protected-request IAM calls, and leaves directory reads on the user's bearer in the matching test environment. Existing rows fail explicitly until a production owner or authorized manager supplies the imported test credential. The repair API verifies credentials before storage, returns metadata only, preserves both environment keys, and advances the version so earlier resolved scopes cannot perform subsequent writes. All 145 workspace tests, formatting, Clippy, database grants, and the OpenAPI check passed locally. The real PostgreSQL regression verifies pairing and rejection paths; separate request tests cover concurrent credential isolation and production selection. Its first test run exposed a 33-character fixture key where 32 were required; correcting that fixture needed no production-code change.

The backend candidate was then exercised locally with PostgreSQL 16 and the existing hosted IAM testing environment. The local row held encrypted imported credentials, while the default application secret was a different dummy value. The unchanged public interpreter plus the candidate CLI completed login, authenticated status, a protected todos read, logout, and an explicit check that the previous access token was inactive. The row was seeded locally, so this run verified real credential selection; the database regression verified the owner pairing endpoint. An additional inactive-token probe with the real paired app credential returned HTTP 200 without issuing a user token. All owned backend, interpreter, database, and relay processes were stopped. Personal IAM configuration, original testing-fixture inputs, Silicon YAML, and the public bundle stayed unchanged. All five GitHub CI jobs passed on each Commit PR, including the backend container build. Both PRs were merged externally during verification: CLI PR #1 at `3fe1812`, and backend PR #2 at `cbe3cd1`. At 07:29 UTC the hosted backend still reported `06c15dd`; a later check at 07:31 reported the new `cbe3cd1` build with healthy readiness. That rollout was external to this work.

The retained hosted Commit sandbox initially returned the expected explicit pairing error. Its production owner and active membership were verified live before supplying its already-imported IAM app credential to the new pairing endpoint. The endpoint returned metadata only, advanced the sandbox from version 1 to 2, preserved its testing key, and retained its linked IAM environment. No new external resources or application content were created. The public interpreter and original bundled Commit CLI then passed login, authenticated status, and a protected read; removal still failed because that CLI lacked logout. The corrected CLI removed that retained session and completed a fresh full login/read/logout cycle. Both previous access tokens were explicitly inactive afterward. The fixture's separate Carbon control session was also revoked and removed; the personal IAM session was never refreshed and its files were unchanged.

### Preparing the 3.5.1 candidate

The final completion audit found three concrete runtime gaps. First, an event or heartbeat blocked in a Bash expression could look up its Silicon ID again after reconnect and deliver old work into a replacement configuration. The same identity reuse affected captured ISI capabilities and delayed ephemeral replies when a persistent UUID was restored. Dispatch now keeps the original connection, capabilities and replies retain a weak reference to their exact worker, and heartbeat deadlines are keyed by connection generation. A blocked-Bash regression exercises both event flow and the real scheduler, verifies the old flow's catch still runs, and proves the replacement receives no old message or deadline. Another regression retires workers and reconnects with restored UUIDs to confirm old capabilities stay invalid.

Second, relative `disconnect` paths were resolved in the daemon's working directory. A test with identically named YAML files in separate directories reproduced the public 3.5.0 binary disconnecting the wrong Silicon, even when the requested file did not exist in the caller's directory. The CLI now canonicalizes paths before the request; the API accepts IDs or absolute paths. The E2E checks missing files, spaces, symlinks, and independent connected homes. Third, `--new` bypassed the title requirement for new session-mode ephemeral work. The shared creation check now enforces it; automatic flow creation uses its session ID as the title, and existing work still accepts titleless follow-ups.

The candidate passed 27 Rust tests, formatting, Clippy, and the complete real Omni/Caddy E2E with its scripted provider, including all new paths. The source version was prepared as 3.5.1. Commit's validated logout merge had exactly the same Git tree as the tested CLI candidate, so its exact revision could be bundled without waiting for a crates.io release. The source installer pinned that revision and checked for `logout` before activation. At that stage, public release 3.5.0 and its assets remained unchanged; no new service or documentation deployment was performed during the follow-up.

A complete macOS ARM64 source installation then built Silicon 3.5.1, reused the verified released dependencies, and included a fresh Cargo build of Commit's exact Git revision. All 16 payload executables/wrappers were present, every exposed command ran, all six IAM discovery commands returned their app IDs, and the installed Commit wrapper exposed logout. That installed bundle passed the entire real Omni/Caddy E2E again and was packaged locally with its installer and notices. Reusing a managed bundle requires the native Commit/Remind executables as source inputs, because the installer creates their wrappers; the guide now makes that explicit. Installer regressions also verify the exact Git fallback and refuse an old offline Commit binary without changing the active installation.

The existing release workflow then built exact source `f3131299ea01613b8585ba9389fb1ad6abf8e140` with `build_only=true`. [All four native jobs](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/34453877850) passed on macOS 15 and Ubuntu 24.04, each on ARM64 and x86-64. Every job passed formatting, Rust tests, Clippy, complete source installation, the installed real Omni/Caddy E2E, bundled CLI execution including Commit logout help, and all six IAM application-discovery checks. Each uploaded its platform bundle and installer. The draft-release job was skipped; this verification created no tag, public release, or deployment.

All four downloaded artifact ZIPs matched GitHub's SHA-256 digests. Each bundle contained the expected 30 regular payload files with the required executable modes and native architectures, a 3.5.1 version marker, and the exact source installer and notices. No Silicon YAML or runtime state was included.

The actual macOS ARM64 CI archive was also served through a local HTTPS test endpoint and installed into a fresh isolated prefix with Cargo absent from `PATH`. The installed interpreter reported Silicon 3.5.1, all 30 payload files matched the CI archive byte for byte, and the installed Commit wrapper exposed logout. This checked the candidate's binary download and activation path. That validation changed neither public release 3.5.0 nor the deployed documentation.

### Publishing 3.5.1

After publication was authorized, [PR #7](https://github.com/teamofsilicons/silicon-stemcell/pull/7) merged as `0ea64c1`. The installation links and release-facing documentation were updated, and `v3.5.1` was tagged at `1ad23cf4a8ce062522dfa7b3d9fb9f226c6e5bfa`. The tagged runtime, installer, dependency inputs, tests, workflow, and notices are byte-for-byte identical to the four-platform build source `f3131299`. Both checks on the [tagged source](https://github.com/teamofsilicons/silicon-stemcell/actions/runs/34459210895) passed.

The six verified artifacts were uploaded to a draft release. Every uploaded size and GitHub SHA-256 digest matched the local files. The release workflow was disabled only while pushing the tag to promote those already-verified native builds, and was immediately restored to its active state. [Silicon 3.5.1](https://github.com/teamofsilicons/silicon-stemcell/releases/tag/v3.5.1) was published as the latest stable release on 10 September 2026 at 09:13:38 UTC. The earlier 3.5.0 release and its assets were retained unchanged.

All six canonical release URLs were then downloaded without GitHub authentication. Every request returned HTTP 200, and every downloaded file matched both the verified local artifact and GitHub's recorded digest and size. A separate run fetched the public installer with the system curl and installed into a fresh macOS ARM64 prefix, with source/version/mirror overrides removed and Cargo absent from `PATH`. The initial requests were slow but completed successfully. The interpreter reported `silicon 3.5.1`, all 30 installed payload files matched the native CI archive byte for byte, and the installed Commit wrapper exposed logout. This verified the actual public download and installation path without changing the user's shell profile or existing installation.

The documentation uses the existing `silicon-docs` Vercel project at `docs.teamofsilicons.com`. Its version badge, installation command, guide, and diary now describe 3.5.1 and these release checks. The deployment contains the committed static `docs/site` output; the interpreter continues to run on each user's machine. No new runtime server or backend rollout was needed for this release.
